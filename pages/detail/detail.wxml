<!-- 轮播 -->
<view class="detail_hd" style="background: #fff;">
  <swiper class="swiper_box" style="{{swiperHeight}}" indicator-color="#fff" indicator-active-color="{{primaryColor}}"
    indicator-dots="{{detail.imagelist.length > 1 && swiperTrue}}" autoplay="{{swiperTrue}}" circular="{{swiperTrue}}" interval="3000" duration="500">
    <block wx:for="{{detail.imagelist}}" wx:key="item.id">
      <swiper-item>
        <image mode="widthFix" src="{{item.sourceProductImagePath}}" bindload="imageLoad" bindtap="showPic" data-src="{{item.sourceProductImagePath}}" />
      </swiper-item>
    </block>
  </swiper>
  <view class="detail_title">
    {{detail.name}}
    <view class="prodtype">型号：{{detail.prodtype}}</view>
    <view class="price">￥{{detail.price}}</view>
  </view>
</view>
<view class='separator'></view>
<!-- 切换 -->
<view class="weui-navbar">
  <view class="weui-navbar__item {{nav == '1' ? 'on' : ''}}" bindtap="nav" data-nav="1">
    图文详情
  </view>
  <view class="weui-navbar__item {{nav == '2' ? 'on' : ''}}" bindtap="nav" data-nav="2" style="border-right:none">
    参数
  </view>
</view>

<view style="background: #fff;margin-bottom:2.5rem">
  <!-- 图文详情 -->
  <view hidden="{{nav != '1'}}">
    <view class="detail_bd">
      <rich-text nodes="{{detail.proddesc}}" bindtap="showDescPic"></rich-text>
    </view>
  </view>
  <!-- 参数  -->
  <view hidden="{{nav != '2'}}">
    <view class="detail_bd">
      <rich-text nodes="{{detail.detail1}}" bindtap="showDescPic"></rich-text>
    </view>
  </view>
</view>
<view class="detail-btn-contcat" style="background: {{primaryColor}};color:#fff">
  <contact-button type="default-light" size="27" session-from="{{detail.name}}"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:27px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:54px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:81px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:108px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:135px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:162px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:189px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:216px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:243px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:270px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:297px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:324px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:351px;"></contact-button>
  <contact-button type="default-light" size="27" session-from="{{detail.name}}" style="left:378px;"></contact-button>
  询盘
</view>
